# 场景打磨（宣传图对照）2026-08-16

对照 `promo/keyart-farm-square.jpg` 与现网截图 `_verify-keyart-new.png`。
不改存档坐标、不改经济、不部署。

## 验收

1. [x] 水塘/溪：一整汪青蓝水，岸暖，**没有**每格白线圈。
2. [x] 田：2026-08-17 Chris「不要连太紧」后改为**独立木框苗床**（`BED_W=0.78`，中间露草，锁地只留淡描边）；锁只留「下一档」一枚淡锁；空床无脉冲 `+`。不要再合成一整片土。
3. [x] 缩放钮：半透明玻璃，不抢黄昏天空。
4. [x] 摊前路人 / 沿路行人：手绘小人，不再是 emoji 头。
5. [x] 节日/未重绘叶菜走已有油画贴图，不再飘 emoji。
5b. [x] 2026-08-17：开局番茄/茄子/白菜/小葱成熟图改成**土里长的植株**（不是菜篮）。缺生长帧的菜用成熟图缩小冒出，不掉 SVG。抠绿必须按幕布 RGB 距离，HSV 色相会吃掉绿叶。
6. [x] 新号截图 `_verify-keyart-new.png`、老号 `_verify-keyart-old.png` 对照宣传图。
7. [x] `node --check src/js/mapview-iso.js` 通过。
8. [x] 老存档房子/菜地坐标不变。
